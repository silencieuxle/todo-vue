// We will now define modal id here with a better name.
// Use it like this:    this.$bvModal.show(ModalRegister.AdvancedTodoModal);
// Instead of:          this.$bvModal.show('advancedTodoModal');

const ModalRegister = {
  AdvancedTodoModal: 'advancedTodoModal',
};

export default ModalRegister;
